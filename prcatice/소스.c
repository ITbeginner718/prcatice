#include <stdio.h> //라이브러리 생성

int main() //메인 함수 생성 
{
	/*
	//printf() 명령문 출력 
	int var = 0;
	scanf_s("%d",&var);
	printf("var:%d \n",var);

	float flo = 0.0;
	scanf_s("%f",&flo);
	printf("var:%.1f \n", flo); //형식 문자열 앞에 .1를 붙히면 첫째 짜리 소수점 까지만 출력 

	double dou = 0.0;
	scanf_s("%lf", &dou);
	printf("dou:%.2lf \n", dou);
	

	char ch = 0;
	scanf_s("%c", &ch,1); //_s:안전하게 문자를 입력받는 뜻으로 항상 문자에 대한 인수를 전달해야 함 
	printf("ch:%c \n", ch);
	

	char ch_s[20] = { 0 };
	scanf_s("%s", ch_s, sizeof(ch_s)); //scanf:엔터 및 띄어쓰기로 문자 입력 종료 
	printf(ch_s,"종료\n");
	

	char ch_s[20] = { 0 };
	gets_s(ch_s, sizeof(ch_s)); //gets:엔터로 문자 입력 종료
	printf(ch_s);
	*/
	return 0;
}